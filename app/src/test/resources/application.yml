app:
    jpa.enabled: true
    quartz.enabled: true
    api.swagger.enabled: false

    jms:
        enabled: true
        storeType: persistent
        store-path: build/jms

    base-packages:
        - com.proitr
