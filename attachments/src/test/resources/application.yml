app:
    jpa.enabled: true
    api.swagger.enabled: false

    jms:
        enabled: false
        storeType: memory

    attachments:
        enabled: true
        store-type: directory
        minio:
            endpoint: http://minio.dnit:9090/
            access-key: dev
            secret-key: barbecue
            bucket: uploads
        directory:
            path: uploads